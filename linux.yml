---
  - hosts: all
    become: yes
    tasks:
        - name: httpd package install
          yum:
              name: httpd
              state: present
        
        - name: Start service httpd, if not running
          service:
           name: httpd
           state: started
