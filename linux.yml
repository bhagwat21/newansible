---
  - hosts: all
    become: yes
    tasks:
  - name: Install the latest version of Apache
    yum:
       name: httpd
       state: latest

  - name: Install Apache >= 2.4
    yum:
     name: httpd>=2.4
     state: present
    