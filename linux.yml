---
  - hosts: all
    become: yes
    tasks:
        - name: httpd package install on nodes
          yum:
              name: httpd
              state: present
        - name: starting httpd service
          service:
                 name: httpd
                 state: started
                 enable: yes  
